# Postpublication data-availability template — V13

Replace every bracketed field only after a live verification.

The human-participant dataset analyzed in this study is available from SWISSUbase/LaRS as *A multimodal speech-production dataset with time-aligned articulography, EEG, audio, and vocal-tract anatomy*, Version 1.2 (version-specific DOI `10.48656/vc7s-pt02`; canonical DOI `10.48656/6y1s-px92`). A reproducibility repository is publicly available at `[PUBLIC GITHUB URL]`, fixed at commit `[IMMUTABLE COMMIT SHA]` and release `[RELEASE TAG]`. An archival snapshot is available at `[ARCHIVAL DOI, IF ONE EXISTS; OTHERWISE REMOVE THIS SENTENCE]`.

The repository includes `[EXACT INCLUDED CODE SCOPE]` and `[EXACT DEIDENTIFIED DERIVED-TABLE/OUTPUT SCOPE]`, including the corrected V2.4 ArtiSynth patch and validated Phase C tables. It excludes raw audio, EMA, EEG/BDF, imaging, direct identifiers, and obsolete/superseded analyses. Raw assistant-by-repetition anthropometric readings were not preserved, so repeatability statistics cannot be reconstructed. First-principles EMA/audio extraction is only partially documented because raw signals, the named cycle-aggregation script, and complete historical run records are not all available.
